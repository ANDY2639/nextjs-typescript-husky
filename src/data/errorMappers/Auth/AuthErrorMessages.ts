export enum errorAuthMessage {
  INVALID_OTP_CODE = "Los datos ingresados no coinciden, te quedan ValidAttempts intentos.",
}

export enum errorAuthTitle {
  INVALID_OTP_CODE = "Acceso incorrecto.",
}
